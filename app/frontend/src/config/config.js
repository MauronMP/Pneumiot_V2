// Define the base URLs for your API and frontend
const config = {
    apiBaseUrl: 'http://backend:3000/api/',   // Base URL for API calls
    frontendBaseUrl: 'http://backend:3000/api/frontend/',  // Base URL for frontend routes
    apiV1: 'http://backend:3000/api/v1/'  // Base URL for frontend routes
  };
  
  export default config;
  