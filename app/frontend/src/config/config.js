// Define the base URLs for your API and frontend
const config = {
    apiBaseUrl: 'http://localhost:3000/api/',   // Base URL for API calls
    frontendBaseUrl: 'http://localhost:3000/api/frontend/',  // Base URL for frontend routes
    apiV1: 'http://localhost:3000/api/v1/'  // Base URL for frontend routes
  };
  
  export default config;
  