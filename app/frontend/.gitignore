# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules       # Ignore the node_modules directory, which contains project dependencies installed via npm or yarn
/.pnp               # Ignore the .pnp directory, related to Yarn PnP (Plug'n'Play) feature for module resolution
.pnp.js             # Ignore the .pnp.js file, used for Yarn's PnP configuration

# testing
/coverage           # Ignore the coverage directory that contains code coverage reports generated by testing tools

# production
/build              # Ignore the build directory, typically generated by bundlers (e.g., Webpack) during production builds

# misc
.DS_Store           # Ignore the .DS_Store file, automatically created by macOS for directory metadata
.env.local          # Ignore environment variable files specific to local development environments
.env.development.local
.env.test.local
.env.production.local

# These are environment configuration files that should not be committed to version control as they contain sensitive or local-specific information
npm-debug.log*      # Ignore npm debug logs, which can be generated if npm commands fail
yarn-debug.log*     # Ignore Yarn debug logs, similar to npm debug logs
yarn-error.log*     # Ignore Yarn error logs, which contain details about Yarn command failures
